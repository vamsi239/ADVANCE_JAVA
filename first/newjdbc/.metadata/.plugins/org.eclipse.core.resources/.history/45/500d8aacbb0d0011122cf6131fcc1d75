package M1Question19feb;

import java.util.*;

public class productMS {
	   private Map<String, List<Double>> map = new HashMap<>();
	   
	   public void addProduct(String product, Double price) {
		   
		   if(!map.containsKey(product)) {
			   map.put(product, new ArrayList());
		   }
		   map.get(product).add(price);
		   System.out.println("Product added"+product+" "+price);
	   }
	   
      public void removeProduct(String product) {
    	  map.remove(product);
		   
	   }
	   public String getPattern(String product) {
		  List<Double> prices =  map.get(product);
		  if (prices == null || prices.size() < 2)
	            return "NO_PATTERN";
		  
		  boolean inc = true;
		  boolean dec = true;
		  boolean eq =true;
		  

	        for (int i = 1; i < prices.size(); i++) {
			  if(prices.get(i)<=prices.get(i-1)) {
				  inc = false;
			  }
			  if(prices.get(i)>=prices.get(i-1)) {
				  dec = false;
			  }
			  if(prices.get(i)!=prices.get(i-1)) {
				  eq = false;
			  }
	        }
			  if(inc) return "Increasing";
			  if(dec) return "Decreasing";
			  if(eq) return "Equal";
			  if(isPeak(prices)) {
				  return "Peak";
			  
		      }
			  return "NO_PATTERN";

	   }
	   boolean isPeak(List<Double> prices) {
		   int i =1;
		   while(i<prices.size()&&prices.get(i-1)<prices.get(i)) i++;
		   if (i == 1 || i == prices.size())
	            return false;
		   while(i<prices.size()&&prices.get(i-1)>prices.get(i)) i++;
		   return i==prices.size();
	   }
	   
	   
	   public static void main(String[] args) {
		  productMS pm = new productMS();
		 //  Scanner sc =  new Scanner(System.in);
		   
//		   int n = sc.nextInt();
//		   sc.nextLine();
//		   for(int i =0;i<n;i++) {
//			   String s = sc.nextLine().trim();
//			   
//			   String[] parts = s.split(" ");
//			   if(parts[0]=="ADD") {
//				   p.addProduct(parts[1], Double.parseDouble(parts[2]));
//			   }else if(parts[0] =="RM") {
//				   p.removeProduct(parts[1]);
//			   }else if(parts[0]=="get") {
//				   p.getPattern(parts[1]);
//			   }
//		   }

	        // INCREASING
	        pm.addProduct("Laptop", 50000.0);
	        pm.addProduct("Laptop", 52000.0);
	        pm.addProduct("Laptop", 54000.0);

	        // DECREASING
	        pm.addProduct("Phone", 30000.0);
	        pm.addProduct("Phone", 25000.0);
	        pm.addProduct("Phone", 20000.0);

	        // EQUAL
	        pm.addProduct("Tablet", 20000.0);
	        pm.addProduct("Tablet", 20000.0);
	        pm.addProduct("Tablet", 20000.0);

	        // PEAK
	        pm.addProduct("Watch", 1000.0);
	        pm.addProduct("Watch", 1500.0);
	        pm.addProduct("Watch", 2000.0);
	        pm.addProduct("Watch", 1200.0);

	        // NO_PATTERN
	        pm.addProduct("Camera", 10000.0);
	        pm.addProduct("Camera", 15000.0);
	        pm.addProduct("Camera", 12000.0);
	        pm.addProduct("Camera", 18000.0);

	        System.out.println("Laptop: " + pm.getPattern("Laptop"));
	        System.out.println("Phone: " + pm.getPattern("Phone"));
	        System.out.println("Tablet: " + pm.getPattern("Tablet"));
	        System.out.println("Watch: " + pm.getPattern("Watch"));
	   }

}
